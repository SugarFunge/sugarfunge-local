table:
  schema: sf
  name: escrow_class
array_relationships:
  - name: escrows
    using:
      foreign_key_constraint_on:
        column: escrow_class_id
        table:
          schema: sf
          name: escrow
