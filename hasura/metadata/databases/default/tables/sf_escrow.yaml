table:
  schema: sf
  name: escrow
array_relationships:
  - name: escrow_shares
    using:
      foreign_key_constraint_on:
        column: escrow_id
        table:
          schema: sf
          name: escrow_share
